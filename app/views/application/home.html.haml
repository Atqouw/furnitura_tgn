= render(partial: 'application/partials/swiper')
.ui.bottom.attached.success.message
  = render(partial: 'application/partials/steps')

.ui.container
  %h2.ui.dividing.header
    Все товары
  .ui.four.doubling.cards
    = render(partial: 'items/partials/item_card', collection: @items)
  = paginate(@items, theme: 'default')
